// Auto-generated by npu/rtlgen/gen_llama7b_rmsnorm.py
module llama7b_rmsnorm_phase3_macro_banked_pipelined_l16_ng45 (
    input  wire                         clk,
    input  wire                         rst_n,
    input  wire                         in_valid,
    output wire                         in_ready,
    input  wire [255:0] in_row,
    input  wire [255:0] in_gamma,
    input  wire                         in_last,
    output wire                         out_valid,
    input  wire                         out_ready,
    output wire [255:0] out_row,
    output wire                         out_last,
    output wire                         out_protocol_error,
    output reg  [31:0]                  accepted_row_count,
    output reg  [31:0]                  completed_row_count
);
  localparam integer HIDDEN_SIZE = 4096;
  localparam integer LANES = 16;
  localparam integer BEATS = 256;
  localparam integer BEAT_W = 8;
  localparam [3:0] ACCEPT_MAX = 4'd0, ACCUMULATE_REPLAY = 4'd1,
      F0_NORMALIZE = 4'd2, F1_SEED = 4'd3, F2_Y_SQUARE = 4'd4,
      F3_M_Y_SQUARE = 4'd5, F4_UPDATE = 4'd6, EMIT = 4'd7, DRAIN = 4'd8;

  reg [3:0] state;
  reg [BEAT_W-1:0] input_beat;
  reg [BEAT_W-1:0] replay_beat;
  reg [BEAT_W-1:0] issue_beat;
  reg [BEAT_W:0] store_issue_count;
  reg [2:0] store_reads_inflight;
  wire [2:0] arithmetic_occupancy;
  wire store_write_en = state == ACCEPT_MAX && in_valid && in_ready;
  wire store_read_en;
  wire [BEAT_W-1:0] store_read_beat = store_issue_count[BEAT_W-1:0];
  wire store_read_valid;
  wire [(LANES*16)-1:0] store_read_row;
  wire [(LANES*16)-1:0] store_read_gamma;
  wire store_request_collision;

  llama7b_rmsnorm_banked_row_gamma_store u_row_gamma_store (
      .clk(clk),
      .rst_n(rst_n),
      .write_en(store_write_en),
      .write_beat(input_beat),
      .write_row(in_row),
      .write_gamma(in_gamma),
      .read_en(store_read_en),
      .read_beat(store_read_beat),
      .read_valid(store_read_valid),
      .read_row(store_read_row),
      .read_gamma(store_read_gamma),
      .request_collision(store_request_collision)
  );

  reg row_has_nonzero;
  reg signed [10:0] max_square_exponent;
  reg sticky_protocol_error;
  reg row_protocol_error;
  reg [47:0] accumulator_mantissa;
  reg signed [10:0] accumulator_lsb_exponent;

  reg [25:0] variance_mantissa_q24;
  reg signed [9:0] variance_exponent_even;
  reg [20:0] seed_q20;
  reg [20:0] y_square_q20;
  reg [21:0] correction_q20;
  reg [20:0] rsqrt_mantissa_q20;
  reg signed [8:0] rsqrt_exponent;

  reg s0_valid, s0_last, s0_error;
  reg [(LANES*28)-1:0] s0_product;
  reg [(LANES*10)-1:0] s0_exponent;
  reg [(LANES*16)-1:0] s0_gamma;
  reg [LANES-1:0] s0_sign;
  reg s1_valid, s1_last, s1_error;
  reg [(LANES*36)-1:0] s1_coefficient;
  reg [(LANES*10)-1:0] s1_exponent;
  reg [LANES-1:0] s1_sign;
  reg s2_valid, s2_last, s2_error;
  reg [(LANES*16)-1:0] s2_data;

  wire s2_ready = !s2_valid || out_ready;
  wire s1_ready = !s1_valid || s2_ready;
  wire s0_ready = !s0_valid || s1_ready;
  wire issue_valid = 1'b0;
  assign arithmetic_occupancy = {2'b0, s0_valid} + {2'b0, s1_valid} + {2'b0, s2_valid};
  assign store_read_en =
      (store_issue_count < BEATS) &&
      ((state == ACCUMULATE_REPLAY) ||
       (state == EMIT && (arithmetic_occupancy + store_reads_inflight < 3)));

  assign in_ready = state == ACCEPT_MAX;
  assign out_valid = s2_valid;
  assign out_row = s2_data;
  assign out_last = s2_last;
  assign out_protocol_error = s2_error;

  integer lane;
  integer bit_index;
  integer variance_lead;
  integer variance_shift;
  integer memory_index;
  reg [15:0] word_value;
  reg [15:0] coefficient_value;
  reg signed [10:0] term_exponent_value;
  reg signed [10:0] candidate_high;
  reg signed [10:0] beat_max;
  reg signed [10:0] combined_max;
  reg beat_has_nonzero;
  reg beat_invalid;
  reg [39:0] beat_sum;
  reg [35:0] aligned_term;
  reg [48:0] accumulator_next;
  reg signed [10:0] mean_lsb_exponent;
  reg signed [10:0] variance_base_exponent;
  reg signed [10:0] variance_leading_exponent;
  reg signed [10:0] variance_even_temp;
  reg [319:0] variance_exact;
  reg [26:0] variance_rounded;
  reg [95:0] arithmetic_value;
  reg [95:0] arithmetic_rounded;
  reg [41:0] y_square_product;
  reg [46:0] m_y_square_product;
  reg [42:0] update_product;
  reg [21:0] half_m_y_square;
  reg [21:0] update_with_bias;
  reg [7:0] gamma_significand;
  reg signed [10:0] gamma_exponent;

  function automatic [7:0] bf16_significand;
    input [15:0] value;
    begin
      if (value[14:7] == 8'd0)
        bf16_significand = {1'b0, value[6:0]};
      else
        bf16_significand = {1'b1, value[6:0]};
    end
  endfunction

  function automatic signed [10:0] bf16_exponent;
    input [15:0] value;
    begin
      if (value[14:7] == 8'd0)
        bf16_exponent = -11'sd133;
      else
        bf16_exponent = $signed({3'd0, value[14:7]}) - 11'sd134;
    end
  endfunction

  function automatic [15:0] square_coefficient;
    input [15:0] value;
    reg [7:0] significand;
    begin
      significand = bf16_significand(value);
      square_coefficient = significand * significand;
    end
  endfunction

  function automatic signed [10:0] square_term_exponent;
    input [15:0] value;
    begin
      square_term_exponent = bf16_exponent(value) <<< 1;
    end
  endfunction

  function automatic [4:0] leading_index_16;
    input [15:0] value;
    integer index;
    begin
      leading_index_16 = 5'd0;
      for (index = 0; index < 16; index = index + 1)
        if (value[index]) leading_index_16 = index[4:0];
    end
  endfunction

  function automatic [5:0] leading_index_36;
    input [35:0] value;
    integer index;
    begin
      leading_index_36 = 6'd0;
      for (index = 0; index < 36; index = index + 1)
        if (value[index]) leading_index_36 = index[5:0];
    end
  endfunction

  function automatic [35:0] align_square_rne;
    input [15:0] coefficient;
    input signed [10:0] term_exponent;
    input signed [10:0] target_exponent;
    integer amount;
    integer index;
    reg [35:0] quotient;
    reg guard_bit;
    reg sticky_bit;
    begin
      amount = target_exponent - term_exponent;
      quotient = 36'd0;
      guard_bit = 1'b0;
      sticky_bit = 1'b0;
      if (coefficient == 0) begin
        quotient = 36'd0;
      end else if (amount <= 0) begin
        quotient = {20'd0, coefficient} << (-amount);
      end else if (amount <= 16) begin
        quotient = {20'd0, coefficient >> amount};
        guard_bit = coefficient[amount-1];
        for (index = 0; index < 15; index = index + 1)
          if (index < amount-1) sticky_bit = sticky_bit | coefficient[index];
        if (guard_bit && (sticky_bit || quotient[0])) quotient = quotient + 1'b1;
      end
      align_square_rne = quotient;
    end
  endfunction

  function automatic [26:0] rne_shift_320_to_27;
    input [319:0] value;
    input integer amount;
    integer index;
    reg [320:0] quotient;
    reg guard_bit;
    reg sticky_bit;
    begin
      quotient = 321'd0;
      guard_bit = 1'b0;
      sticky_bit = 1'b0;
      if (amount <= 0) begin
        quotient = {1'b0, value} << (-amount);
      end else if (amount <= 320) begin
        quotient = {1'b0, value >> amount};
        guard_bit = value[amount-1];
        for (index = 0; index < 319; index = index + 1)
          if (index < amount-1) sticky_bit = sticky_bit | value[index];
        if (guard_bit && (sticky_bit || quotient[0])) quotient = quotient + 1'b1;
      end
      rne_shift_320_to_27 = quotient[26:0];
    end
  endfunction

  function automatic [95:0] rne_shift_96;
    input [95:0] value;
    input integer amount;
    integer index;
    reg [95:0] quotient;
    reg guard_bit;
    reg sticky_bit;
    begin
      quotient = 96'd0;
      guard_bit = 1'b0;
      sticky_bit = 1'b0;
      if (amount <= 0) begin
        quotient = value << (-amount);
      end else if (amount <= 96) begin
        quotient = value >> amount;
        guard_bit = value[amount-1];
        for (index = 0; index < 95; index = index + 1)
          if (index < amount-1) sticky_bit = sticky_bit | value[index];
        if (guard_bit && (sticky_bit || quotient[0])) quotient = quotient + 1'b1;
      end
      rne_shift_96 = quotient;
    end
  endfunction

  function automatic [36:0] rne_shift_36;
    input [35:0] value;
    input integer amount;
    integer index;
    reg [36:0] quotient;
    reg guard_bit;
    reg sticky_bit;
    begin
      quotient = 37'd0;
      guard_bit = 1'b0;
      sticky_bit = 1'b0;
      if (amount <= 0) begin
        quotient = {1'b0, value} << (-amount);
      end else if (amount <= 36) begin
        quotient = {1'b0, value >> amount};
        guard_bit = value[amount-1];
        for (index = 0; index < 35; index = index + 1)
          if (index < amount-1) sticky_bit = sticky_bit | value[index];
        if (guard_bit && (sticky_bit || quotient[0])) quotient = quotient + 1'b1;
      end
      rne_shift_36 = quotient;
    end
  endfunction

  function automatic [15:0] exact_to_bf16;
    input sign_value;
    input [35:0] coefficient;
    input signed [10:0] exponent_value;
    integer leading_index;
    integer leading_exponent;
    integer amount;
    reg [36:0] rounded;
    reg [8:0] significand;
    reg [7:0] exponent_field;
    begin
      if (coefficient == 0) begin
        exact_to_bf16 = {sign_value, 15'd0};
      end else begin
        leading_index = leading_index_36(coefficient);
        leading_exponent = leading_index + exponent_value;
        if (leading_exponent < -126) begin
          amount = -133 - exponent_value;
          rounded = rne_shift_36(coefficient, amount);
          if (rounded < 128)
            exact_to_bf16 = {sign_value, 8'd0, rounded[6:0]};
          else
            exact_to_bf16 = {sign_value, 8'd1, 7'd0};
        end else begin
          rounded = rne_shift_36(coefficient, leading_index - 7);
          significand = rounded[8:0];
          if (significand == 9'd256) begin
            significand = 9'd128;
            leading_exponent = leading_exponent + 1;
          end
          if (leading_exponent > 127)
            exact_to_bf16 = {sign_value, 8'hff, 7'd0};
          else begin
            exponent_field = leading_exponent + 127;
            exact_to_bf16 = {sign_value, exponent_field, significand[6:0]};
          end
        end
      end
    end
  endfunction

  function automatic [20:0] seed_rom;
    input [7:0] address;
    begin
      case (address)
      8'd0: seed_rom = 21'h0ff018;
      8'd1: seed_rom = 21'h0fd0d4;
      8'd2: seed_rom = 21'h0fb245;
      8'd3: seed_rom = 21'h0f9465;
      8'd4: seed_rom = 21'h0f772d;
      8'd5: seed_rom = 21'h0f5a97;
      8'd6: seed_rom = 21'h0f3e9d;
      8'd7: seed_rom = 21'h0f233a;
      8'd8: seed_rom = 21'h0f0868;
      8'd9: seed_rom = 21'h0eee23;
      8'd10: seed_rom = 21'h0ed466;
      8'd11: seed_rom = 21'h0ebb2c;
      8'd12: seed_rom = 21'h0ea272;
      8'd13: seed_rom = 21'h0e8a32;
      8'd14: seed_rom = 21'h0e7269;
      8'd15: seed_rom = 21'h0e5b14;
      8'd16: seed_rom = 21'h0e442e;
      8'd17: seed_rom = 21'h0e2db4;
      8'd18: seed_rom = 21'h0e17a3;
      8'd19: seed_rom = 21'h0e01f8;
      8'd20: seed_rom = 21'h0decb0;
      8'd21: seed_rom = 21'h0dd7c8;
      8'd22: seed_rom = 21'h0dc33d;
      8'd23: seed_rom = 21'h0daf0c;
      8'd24: seed_rom = 21'h0d9b33;
      8'd25: seed_rom = 21'h0d87af;
      8'd26: seed_rom = 21'h0d747f;
      8'd27: seed_rom = 21'h0d619f;
      8'd28: seed_rom = 21'h0d4f0e;
      8'd29: seed_rom = 21'h0d3cc9;
      8'd30: seed_rom = 21'h0d2acf;
      8'd31: seed_rom = 21'h0d191d;
      8'd32: seed_rom = 21'h0d07b2;
      8'd33: seed_rom = 21'h0cf68b;
      8'd34: seed_rom = 21'h0ce5a8;
      8'd35: seed_rom = 21'h0cd506;
      8'd36: seed_rom = 21'h0cc4a3;
      8'd37: seed_rom = 21'h0cb47f;
      8'd38: seed_rom = 21'h0ca498;
      8'd39: seed_rom = 21'h0c94eb;
      8'd40: seed_rom = 21'h0c8578;
      8'd41: seed_rom = 21'h0c763e;
      8'd42: seed_rom = 21'h0c673b;
      8'd43: seed_rom = 21'h0c586d;
      8'd44: seed_rom = 21'h0c49d4;
      8'd45: seed_rom = 21'h0c3b6e;
      8'd46: seed_rom = 21'h0c2d3b;
      8'd47: seed_rom = 21'h0c1f38;
      8'd48: seed_rom = 21'h0c1166;
      8'd49: seed_rom = 21'h0c03c2;
      8'd50: seed_rom = 21'h0bf64c;
      8'd51: seed_rom = 21'h0be903;
      8'd52: seed_rom = 21'h0bdbe6;
      8'd53: seed_rom = 21'h0bcef3;
      8'd54: seed_rom = 21'h0bc22b;
      8'd55: seed_rom = 21'h0bb58c;
      8'd56: seed_rom = 21'h0ba915;
      8'd57: seed_rom = 21'h0b9cc6;
      8'd58: seed_rom = 21'h0b909d;
      8'd59: seed_rom = 21'h0b849b;
      8'd60: seed_rom = 21'h0b78bd;
      8'd61: seed_rom = 21'h0b6d04;
      8'd62: seed_rom = 21'h0b616e;
      8'd63: seed_rom = 21'h0b55fc;
      8'd64: seed_rom = 21'h0b4aab;
      8'd65: seed_rom = 21'h0b3f7d;
      8'd66: seed_rom = 21'h0b346f;
      8'd67: seed_rom = 21'h0b2981;
      8'd68: seed_rom = 21'h0b1eb4;
      8'd69: seed_rom = 21'h0b1405;
      8'd70: seed_rom = 21'h0b0975;
      8'd71: seed_rom = 21'h0aff03;
      8'd72: seed_rom = 21'h0af4af;
      8'd73: seed_rom = 21'h0aea77;
      8'd74: seed_rom = 21'h0ae05c;
      8'd75: seed_rom = 21'h0ad65c;
      8'd76: seed_rom = 21'h0acc78;
      8'd77: seed_rom = 21'h0ac2af;
      8'd78: seed_rom = 21'h0ab900;
      8'd79: seed_rom = 21'h0aaf6b;
      8'd80: seed_rom = 21'h0aa5f0;
      8'd81: seed_rom = 21'h0a9c8e;
      8'd82: seed_rom = 21'h0a9344;
      8'd83: seed_rom = 21'h0a8a13;
      8'd84: seed_rom = 21'h0a80fa;
      8'd85: seed_rom = 21'h0a77f7;
      8'd86: seed_rom = 21'h0a6f0c;
      8'd87: seed_rom = 21'h0a6638;
      8'd88: seed_rom = 21'h0a5d7a;
      8'd89: seed_rom = 21'h0a54d1;
      8'd90: seed_rom = 21'h0a4c3f;
      8'd91: seed_rom = 21'h0a43c1;
      8'd92: seed_rom = 21'h0a3b58;
      8'd93: seed_rom = 21'h0a3304;
      8'd94: seed_rom = 21'h0a2ac4;
      8'd95: seed_rom = 21'h0a2298;
      8'd96: seed_rom = 21'h0a1a80;
      8'd97: seed_rom = 21'h0a127b;
      8'd98: seed_rom = 21'h0a0a89;
      8'd99: seed_rom = 21'h0a02a9;
      8'd100: seed_rom = 21'h09fadc;
      8'd101: seed_rom = 21'h09f321;
      8'd102: seed_rom = 21'h09eb78;
      8'd103: seed_rom = 21'h09e3e0;
      8'd104: seed_rom = 21'h09dc5a;
      8'd105: seed_rom = 21'h09d4e5;
      8'd106: seed_rom = 21'h09cd81;
      8'd107: seed_rom = 21'h09c62d;
      8'd108: seed_rom = 21'h09beea;
      8'd109: seed_rom = 21'h09b7b6;
      8'd110: seed_rom = 21'h09b093;
      8'd111: seed_rom = 21'h09a97f;
      8'd112: seed_rom = 21'h09a27b;
      8'd113: seed_rom = 21'h099b86;
      8'd114: seed_rom = 21'h0994a0;
      8'd115: seed_rom = 21'h098dc8;
      8'd116: seed_rom = 21'h0986ff;
      8'd117: seed_rom = 21'h098045;
      8'd118: seed_rom = 21'h097999;
      8'd119: seed_rom = 21'h0972fa;
      8'd120: seed_rom = 21'h096c6a;
      8'd121: seed_rom = 21'h0965e7;
      8'd122: seed_rom = 21'h095f72;
      8'd123: seed_rom = 21'h095909;
      8'd124: seed_rom = 21'h0952ae;
      8'd125: seed_rom = 21'h094c60;
      8'd126: seed_rom = 21'h09461e;
      8'd127: seed_rom = 21'h093fe9;
      8'd128: seed_rom = 21'h0939c1;
      8'd129: seed_rom = 21'h0933a5;
      8'd130: seed_rom = 21'h092d94;
      8'd131: seed_rom = 21'h092790;
      8'd132: seed_rom = 21'h092197;
      8'd133: seed_rom = 21'h091bab;
      8'd134: seed_rom = 21'h0915c9;
      8'd135: seed_rom = 21'h090ff3;
      8'd136: seed_rom = 21'h090a28;
      8'd137: seed_rom = 21'h090468;
      8'd138: seed_rom = 21'h08feb3;
      8'd139: seed_rom = 21'h08f909;
      8'd140: seed_rom = 21'h08f36a;
      8'd141: seed_rom = 21'h08edd5;
      8'd142: seed_rom = 21'h08e84a;
      8'd143: seed_rom = 21'h08e2ca;
      8'd144: seed_rom = 21'h08dd53;
      8'd145: seed_rom = 21'h08d7e7;
      8'd146: seed_rom = 21'h08d285;
      8'd147: seed_rom = 21'h08cd2d;
      8'd148: seed_rom = 21'h08c7de;
      8'd149: seed_rom = 21'h08c299;
      8'd150: seed_rom = 21'h08bd5d;
      8'd151: seed_rom = 21'h08b82a;
      8'd152: seed_rom = 21'h08b301;
      8'd153: seed_rom = 21'h08ade1;
      8'd154: seed_rom = 21'h08a8ca;
      8'd155: seed_rom = 21'h08a3bc;
      8'd156: seed_rom = 21'h089eb6;
      8'd157: seed_rom = 21'h0899ba;
      8'd158: seed_rom = 21'h0894c6;
      8'd159: seed_rom = 21'h088fda;
      8'd160: seed_rom = 21'h088af7;
      8'd161: seed_rom = 21'h08861c;
      8'd162: seed_rom = 21'h08814a;
      8'd163: seed_rom = 21'h087c7f;
      8'd164: seed_rom = 21'h0877bd;
      8'd165: seed_rom = 21'h087303;
      8'd166: seed_rom = 21'h086e50;
      8'd167: seed_rom = 21'h0869a6;
      8'd168: seed_rom = 21'h086503;
      8'd169: seed_rom = 21'h086067;
      8'd170: seed_rom = 21'h085bd4;
      8'd171: seed_rom = 21'h085747;
      8'd172: seed_rom = 21'h0852c2;
      8'd173: seed_rom = 21'h084e45;
      8'd174: seed_rom = 21'h0849ce;
      8'd175: seed_rom = 21'h08455f;
      8'd176: seed_rom = 21'h0840f7;
      8'd177: seed_rom = 21'h083c96;
      8'd178: seed_rom = 21'h08383c;
      8'd179: seed_rom = 21'h0833e9;
      8'd180: seed_rom = 21'h082f9c;
      8'd181: seed_rom = 21'h082b56;
      8'd182: seed_rom = 21'h082717;
      8'd183: seed_rom = 21'h0822df;
      8'd184: seed_rom = 21'h081ead;
      8'd185: seed_rom = 21'h081a81;
      8'd186: seed_rom = 21'h08165c;
      8'd187: seed_rom = 21'h08123e;
      8'd188: seed_rom = 21'h080e25;
      8'd189: seed_rom = 21'h080a13;
      8'd190: seed_rom = 21'h080607;
      8'd191: seed_rom = 21'h080201;
      default: seed_rom = 21'd0;
      endcase
    end
  endfunction

  function automatic [7:0] variance_rom_address;
    input [25:0] mantissa;
    begin
      variance_rom_address = (mantissa >> 18) - 8'd64;
    end
  endfunction

  always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      state <= ACCEPT_MAX;
      input_beat <= 0;
      replay_beat <= 0;
      issue_beat <= 0;
      store_issue_count <= 0;
      store_reads_inflight <= 0;
      row_has_nonzero <= 1'b0;
      max_square_exponent <= 0;
      sticky_protocol_error <= 1'b0;
      row_protocol_error <= 1'b0;
      accumulator_mantissa <= 0;
      accumulator_lsb_exponent <= 0;
      variance_mantissa_q24 <= 0;
      variance_exponent_even <= 0;
      seed_q20 <= 0;
      y_square_q20 <= 0;
      correction_q20 <= 0;
      rsqrt_mantissa_q20 <= 0;
      rsqrt_exponent <= 0;
      s0_valid <= 1'b0;
      s1_valid <= 1'b0;
      s2_valid <= 1'b0;
      s0_last <= 1'b0;
      s1_last <= 1'b0;
      s2_last <= 1'b0;
      s0_error <= 1'b0;
      s1_error <= 1'b0;
      s2_error <= 1'b0;
      s0_product <= 0;
      s0_exponent <= 0;
      s0_gamma <= 0;
      s0_sign <= 0;
      s1_coefficient <= 0;
      s1_exponent <= 0;
      s1_sign <= 0;
      s2_data <= 0;
      accepted_row_count <= 0;
      completed_row_count <= 0;
    end else begin
      if (store_read_en)
        store_issue_count <= store_issue_count + 1'b1;
      if (store_read_en && !store_read_valid)
        store_reads_inflight <= store_reads_inflight + 1'b1;
      else if (!store_read_en && store_read_valid)
        store_reads_inflight <= store_reads_inflight - 1'b1;
`ifndef SYNTHESIS
      if (store_request_collision) $fatal(1, "RMSNorm storage request collision");
      if (store_read_valid && state == EMIT && !s0_ready)
        $fatal(1, "RMSNorm pipelined macro response exceeded arithmetic credit");
`endif
      if (s2_ready) begin
        s2_valid <= s1_valid;
        if (s1_valid) begin
          s2_last <= s1_last;
          s2_error <= s1_error;
          for (lane = 0; lane < LANES; lane = lane + 1) begin
            if (s1_error)
              s2_data[(lane*16) +: 16] <= 16'h7fc0;
            else
              s2_data[(lane*16) +: 16] <= exact_to_bf16(
                  s1_sign[lane], s1_coefficient[(lane*36) +: 36],
                  $signed(s1_exponent[(lane*10) +: 10]));
          end
        end
      end

      if (s1_ready) begin
        s1_valid <= s0_valid;
        if (s0_valid) begin
          s1_last <= s0_last;
          s1_error <= s0_error;
          for (lane = 0; lane < LANES; lane = lane + 1) begin
            gamma_significand = bf16_significand(s0_gamma[(lane*16) +: 16]);
            gamma_exponent = bf16_exponent(s0_gamma[(lane*16) +: 16]);
            s1_coefficient[(lane*36) +: 36] <=
                s0_product[(lane*28) +: 28] * gamma_significand;
            s1_exponent[(lane*10) +: 10] <=
                $signed(s0_exponent[(lane*10) +: 10]) + gamma_exponent;
            s1_sign[lane] <= s0_sign[lane] ^ s0_gamma[(lane*16)+15];
          end
        end
      end

      if (s0_ready) begin
        s0_valid <= store_read_valid && state == EMIT;
        if (store_read_valid && state == EMIT) begin
          s0_last <= issue_beat == BEATS-1;
          s0_error <= row_protocol_error;
          for (lane = 0; lane < LANES; lane = lane + 1) begin
            word_value = store_read_row[(lane*16) +: 16];
            s0_product[(lane*28) +: 28] <=
                bf16_significand(word_value) * rsqrt_mantissa_q20;
            s0_exponent[(lane*10) +: 10] <=
                bf16_exponent(word_value) + rsqrt_exponent - 11'sd20;
            s0_gamma[(lane*16) +: 16] <= store_read_gamma[(lane*16) +: 16];
            s0_sign[lane] <= word_value[15];
          end
          if (issue_beat == BEATS-1) begin
            issue_beat <= 0;
            state <= DRAIN;
          end else begin
            issue_beat <= issue_beat + 1'b1;
          end
        end
      end

      if (s2_valid && out_ready && s2_last) begin
        completed_row_count <= completed_row_count + 1'b1;
        input_beat <= 0;
        row_has_nonzero <= 1'b0;
        max_square_exponent <= 0;
        sticky_protocol_error <= 1'b0;
        row_protocol_error <= 1'b0;
        accumulator_mantissa <= 0;
        accumulator_lsb_exponent <= 0;
        store_issue_count <= 0;
        store_reads_inflight <= 0;
        state <= ACCEPT_MAX;
      end

      if (state == ACCEPT_MAX && in_valid && in_ready) begin
        beat_has_nonzero = 1'b0;
        beat_invalid = 1'b0;
        beat_max = -11'sd1024;
        for (lane = 0; lane < LANES; lane = lane + 1) begin
          memory_index = input_beat * LANES + lane;
          word_value = in_row[(lane*16) +: 16];
          if (word_value[14:7] == 8'hff || in_gamma[(lane*16)+14 -: 8] == 8'hff) begin
            beat_invalid = 1'b1;
          end else begin
            coefficient_value = square_coefficient(word_value);
            if (coefficient_value != 0) begin
              candidate_high = square_term_exponent(word_value) + leading_index_16(coefficient_value);
              if (!beat_has_nonzero || candidate_high > beat_max) beat_max = candidate_high;
              beat_has_nonzero = 1'b1;
            end
          end
        end
        if (in_last != (input_beat == BEATS-1)) beat_invalid = 1'b1;
        sticky_protocol_error <= sticky_protocol_error | beat_invalid;
        if (beat_has_nonzero && (!row_has_nonzero || beat_max > max_square_exponent))
          max_square_exponent <= beat_max;
        row_has_nonzero <= row_has_nonzero | beat_has_nonzero;

        if (input_beat == BEATS-1) begin
          if (row_has_nonzero && (!beat_has_nonzero || max_square_exponent >= beat_max))
            combined_max = max_square_exponent;
          else if (beat_has_nonzero)
            combined_max = beat_max;
          else
            combined_max = 0;
          max_square_exponent <= combined_max;
          accumulator_lsb_exponent <= (row_has_nonzero || beat_has_nonzero) ? combined_max - 11'sd34 : 0;
          row_protocol_error <= sticky_protocol_error | beat_invalid;
          accepted_row_count <= accepted_row_count + 1'b1;
          input_beat <= 0;
          replay_beat <= 0;
          accumulator_mantissa <= 0;
          state <= ACCUMULATE_REPLAY;
        end else begin
          input_beat <= input_beat + 1'b1;
        end
      end

      if (state == ACCUMULATE_REPLAY && store_read_valid) begin
        beat_sum = 40'd0;
        for (lane = 0; lane < LANES; lane = lane + 1) begin
          word_value = store_read_row[(lane*16) +: 16];
          if (word_value[14:7] != 8'hff) begin
            coefficient_value = square_coefficient(word_value);
            term_exponent_value = square_term_exponent(word_value);
            aligned_term = align_square_rne(
                coefficient_value, term_exponent_value, accumulator_lsb_exponent);
            beat_sum = beat_sum + aligned_term;
          end
        end
        accumulator_next = {1'b0, accumulator_mantissa} + beat_sum;
        accumulator_mantissa <= accumulator_next[47:0];
        if (replay_beat == BEATS-1) begin
          replay_beat <= 0;
          state <= F0_NORMALIZE;
        end else begin
          replay_beat <= replay_beat + 1'b1;
        end
`ifndef SYNTHESIS
        if (accumulator_next[48]) $fatal(1, "RMSNorm 48-bit accumulator overflow");
`endif
      end

      if (state == F0_NORMALIZE) begin
        mean_lsb_exponent = accumulator_lsb_exponent - 11'sd12;
        variance_exact = 320'd0;
        if (mean_lsb_exponent <= -11'sd40) begin
          variance_base_exponent = mean_lsb_exponent;
          variance_exact = {{272{1'b0}}, accumulator_mantissa};
          variance_exact = variance_exact +
              ({{299{1'b0}}, 21'd1099512} << (-40 - mean_lsb_exponent));
        end else begin
          variance_base_exponent = -11'sd40;
          variance_exact = ({{272{1'b0}}, accumulator_mantissa} << (mean_lsb_exponent + 40)) +
              {{299{1'b0}}, 21'd1099512};
        end
        variance_lead = 0;
        for (bit_index = 0; bit_index < 320; bit_index = bit_index + 1)
          if (variance_exact[bit_index]) variance_lead = bit_index;
        variance_leading_exponent = variance_base_exponent + variance_lead;
        if (variance_leading_exponent[0])
          variance_even_temp = variance_leading_exponent - 1'b1;
        else
          variance_even_temp = variance_leading_exponent;
        variance_shift = variance_even_temp - 24 - variance_base_exponent;
        variance_rounded = rne_shift_320_to_27(variance_exact, variance_shift);
        if (variance_rounded == 27'h4000000) begin
          variance_mantissa_q24 <= 26'h1000000;
          variance_exponent_even <= variance_even_temp + 2;
        end else begin
          variance_mantissa_q24 <= variance_rounded[25:0];
          variance_exponent_even <= variance_even_temp[9:0];
        end
        state <= F1_SEED;
      end

      if (state == F1_SEED) begin
        seed_q20 <= seed_rom(variance_rom_address(variance_mantissa_q24));
        state <= F2_Y_SQUARE;
      end

      if (state == F2_Y_SQUARE) begin
        y_square_product = seed_q20 * seed_q20;
        arithmetic_value = {{54{1'b0}}, y_square_product};
        arithmetic_rounded = rne_shift_96(arithmetic_value, 20);
        y_square_q20 <= arithmetic_rounded[20:0];
        state <= F3_M_Y_SQUARE;
      end

      if (state == F3_M_Y_SQUARE) begin
        m_y_square_product = variance_mantissa_q24 * y_square_q20;
        arithmetic_value = {{49{1'b0}}, m_y_square_product};
        arithmetic_rounded = rne_shift_96(arithmetic_value, 25);
        half_m_y_square = arithmetic_rounded[21:0];
        correction_q20 <= 22'd1572864 - half_m_y_square;
        state <= F4_UPDATE;
      end

      if (state == F4_UPDATE) begin
        update_product = seed_q20 * correction_q20;
        arithmetic_value = {{53{1'b0}}, update_product};
        arithmetic_rounded = rne_shift_96(arithmetic_value, 20);
        update_with_bias = arithmetic_rounded[21:0] + 22'd4;
        if (update_with_bias > 22'h1fffff)
          rsqrt_mantissa_q20 <= 21'h1fffff;
        else
          rsqrt_mantissa_q20 <= update_with_bias[20:0];
        rsqrt_exponent <= -($signed(variance_exponent_even) >>> 1);
        issue_beat <= 0;
        store_issue_count <= 0;
        store_reads_inflight <= 0;
        state <= EMIT;
      end
    end
  end
endmodule
