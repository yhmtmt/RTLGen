# Alembic Migrations

This directory is initialized in `cp-001` so the control-plane schema can land in
`cp-002` without restructuring the package.

Expected next step:
- add the initial schema migration after models are defined.
