# Backlog

Backlog items for new navigation should be read from:

- `docs/backlog/index.md`

Canonical backlog location for new work:

- `docs/backlog/items/`

Legacy backlog paths now exist only as compatibility symlinks.
Use `docs/backlog/` for all current navigation and editing.
