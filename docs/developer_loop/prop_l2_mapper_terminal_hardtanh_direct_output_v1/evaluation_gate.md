# Evaluation Gate

Queue the baseline only after local mapper regression, suite generation, and campaign validation pass. Queue the fused candidate only after the baseline evidence is merged and materialized.
