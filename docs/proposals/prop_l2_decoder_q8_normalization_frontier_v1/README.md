# LLM Decoder q8 Normalization Frontier

Proposal workspace for `prop_l2_decoder_q8_normalization_frontier_v1`.

This job tests whether q8 PWL can replace exact normalization with quantized reciprocal normalization without losing the prompt-stress quality gate.
