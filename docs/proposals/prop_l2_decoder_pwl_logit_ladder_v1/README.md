Proposal workspace for `prop_l2_decoder_pwl_logit_ladder_v1`.

This follows the merged PWL failure diagnosis by running a focused precision
ladder on the two broad-v2 shared exact-token misses and nearby controls.
