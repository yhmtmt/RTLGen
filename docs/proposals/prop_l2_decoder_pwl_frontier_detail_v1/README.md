# LLM Decoder PWL Frontier Detail

Proposal workspace for `prop_l2_decoder_pwl_frontier_detail_v1`.

This job deepens the immediate PWL frontier after the survivor cost proxy. It compares only the two exact-safe PWL rows and separates the implementation terms that were collapsed in the previous scalar proxy.
