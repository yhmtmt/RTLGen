# Proposals

Proposal workspaces for new work should be created under:

- `docs/proposals/`

Canonical navigation:

- `docs/proposals/index.md`

Legacy proposal paths now exist only as compatibility symlinks.
Use `docs/proposals/` for all current navigation, bootstrap, and editing.
