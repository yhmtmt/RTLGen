# Proposal Workspace

- `proposal_id`: `prop_l1_terminal_hardsigmoid_retryresume_v1`
- `title`: `Terminal int8 hard-sigmoid retry/resume proof block`

This proposal exists to validate dashboard-side completion recovery on a fresh,
proposal-backed Layer 1 item after intentional evaluator-side submission
interruption.
