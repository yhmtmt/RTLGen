# Evaluation Gate

- status: approved
- approved_by: user
- approved_utc: 2026-03-18T12:05:00Z
- allowed_evaluations:
  - one `measurement_only` Layer 2 item for corrected-contract non-fused `nm1`
    reference points on a small terminal-sensitive softmax-tail suite
  - one `paired_comparison` Layer 2 item for fused vs non-fused `nm1` on the
    same suite
  - no broad ranking sweep yet
- note:
  First stage may use a locally generated ONNX-lite softmax-tail suite because
  the current mapper-supported model subset is narrower than the original
  non-MLP intent.
