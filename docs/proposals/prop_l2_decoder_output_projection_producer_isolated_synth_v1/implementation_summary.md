# Implementation Summary

Adds an isolated producer synth Layer 2 abstraction that reuses `probe_decoder_producer_synth_boundary.py` with `--top gemm_compute_array`, plus nm1 producer config and a clockless synth-only sweep.
