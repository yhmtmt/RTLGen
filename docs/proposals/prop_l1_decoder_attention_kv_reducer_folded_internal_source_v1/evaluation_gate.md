# Evaluation Gate

- Queue after the internal-source wrapper support and focused configs merge.
- Run `runs/campaigns/activations/attention_kv_reducer_folded/sweeps/nangate45_macro_internal_source.json`.
- Treat both `ok` and `failed` rows as boundary evidence because the objective is to separate internal arithmetic feasibility from remaining macro-boundary pressure.
