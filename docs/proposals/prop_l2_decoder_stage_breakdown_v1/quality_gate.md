# Quality Gate

- The estimator must run without generated model artifacts.
- The JSON output must include inputs, assumptions, sweep rows, and a focus summary.
- The markdown report must expose dominant stage and stage shares.
