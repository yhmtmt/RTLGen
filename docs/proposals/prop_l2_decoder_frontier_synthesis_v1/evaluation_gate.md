# Evaluation Gate

Accept the job if it:

- runs the stage breakdown, attention/KV memory, producer/ranker coupling, and synthesis commands
- includes measured attention/KV tile summary in the synthesis output
- reports focus rows with component latencies and dominant component classification
- keeps generated artifact paths repo-portable
