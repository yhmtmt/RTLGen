# Evaluation Gate

Pending remote Nangate45 canary evaluation.

The gate requires the generated-top guard, successful hierarchy-area reports
for every successful metrics row, and `critical_path_ns`, `instance_area_um2`,
`hierarchical_instance_area_um2`, and `total_power_mw` in `metrics.csv`.
