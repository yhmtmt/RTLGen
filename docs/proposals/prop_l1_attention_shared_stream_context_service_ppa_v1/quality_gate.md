# Quality Gate

The service must match the Phase-2 scheduler model for context identity,
producer-supplied addresses, and packet count. RTL must prove exact payloads,
write-once destination coverage, and completion identity under backpressure.
